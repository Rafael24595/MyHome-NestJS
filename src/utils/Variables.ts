import { join } from "path";

export const PathVariables: {private_assets_root: string, tmp_thumbnails:string, collections_audio: string, collections_image: string, collections_video: string} = {
    private_assets_root: '',
    tmp_thumbnails: '',
    collections_audio: '',
    collections_image: '',
    collections_video: ''
}

export const LifeTime: {thumbnail: number} = {
    thumbnail: 43200000
}